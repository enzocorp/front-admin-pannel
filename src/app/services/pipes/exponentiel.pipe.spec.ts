import { ExponentielPipe } from './exponentiel.pipe';

describe('ExponentielPipe', () => {
  xit('create an instance', () => {
    const pipe = new ExponentielPipe();
    expect(pipe).toBeTruthy();
  });
});
