import { ReplaceByPipe } from './replace-by.pipe';

describe('ReplaceByPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceByPipe();
    expect(pipe).toBeTruthy();
  });
});
